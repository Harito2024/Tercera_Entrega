<header>
    <div class="header-container">
        <h1>{{title}}</h1>
        <nav>
            {{#if user}}
            <p>Bienvenido querido {{user.name}}</p>
            <a href="/logout">Cerrar Sesion</a>
            {{else}}
            <a href="/login">Iniciar Sesion</a>
            <a href="/register">Regitrate</a>
            {{/if}}
        </nav>
    </div>
</header>